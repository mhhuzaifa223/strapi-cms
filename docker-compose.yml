services:
  strapi:
    image: strapi/strapi:latest
    container_name: strapi-app
    restart: always
    env_file:
      - .env
    ports:
      - "1337:1337"
    volumes:
      - strapi_data:/srv/app

volumes:
  strapi_data: